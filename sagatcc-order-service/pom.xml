<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.sagatcc.demo</groupId>
        <artifactId>sagatcc-demo</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>sagatcc-order-service</artifactId>
    <name>SagaTcc Order Service</name>

    <dependencies>
        <dependency>
            <groupId>com.sagatcc.demo</groupId>
            <artifactId>sagatcc-wallet-api</artifactId>
        </dependency>
        <dependency>
            <groupId>com.sagatcc</groupId>
            <artifactId>sagatcc-spring-boot-starter</artifactId>
            <version>${sagatcc.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
